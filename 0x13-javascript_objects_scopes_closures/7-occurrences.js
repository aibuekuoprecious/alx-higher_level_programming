#!/usr/bin/node

exports.nbOccurences = function (list, searchElement) {
  const counter = 0;
  for (let i = 0; i < list.length; i++) {
    if (list[i] === searchElement) {
      count++;
    }
  }
  return counter;
};
